<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AndroidDrawingPad" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <!-- Estilo do tema do aplicativo -->
    <style name="AppTheme" parent="Base.Theme.AndroidDrawingPad">
        <!-- Customize the AppTheme here, if needed -->
    </style>

    <!-- Estilo do tema da SplashActivity -->
    <style name="SplashTheme" parent="Base.Theme.AndroidDrawingPad">
        <!-- Customize the SplashTheme here, if needed -->
    </style>
</resources>
